

Want to change or take back the comments you made without any repercussions? Do it here:

<%= form_for @comment  do |f| %>
<p>
	<%= f.label :comment %><br>
	<%= f.text_field :comment %>
</p>
<p><%= f.submit %>
</p>
<% end %>

<%= link_to 'Regret it? Just delete this comment', comment_path(@comment.id), method: :delete %>